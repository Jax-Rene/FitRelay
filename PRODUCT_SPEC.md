# FitRelay（随练 AI）：唯一产品与实施文档

- 版本：v0.3
- 日期：2026-07-12
- 阶段：真实训练验证前 MVP
- 英文名：FitRelay
- 首发：Android / OPPO，Flutter 单代码库，iOS 保持可编译
- 核心文案：**你负责来，今天练什么交给我。**

本文档是“随练 AI”的唯一人工维护文档，同时定义产品目标、用户体验、功能范围、核心规则、技术边界、实施顺序和验收标准。实现时不再依赖其他 Markdown 说明。

仓库中的 `contracts/`、`schema/` 和 `deploy/` 是机器可读的实现资产，不是独立产品文档；若它们与本文冲突，以本文为准并同步修正实现资产。

---

## 1. 产品结论

随练 AI 面向有基础健身经验、但训练频率不稳定的人。它不要求用户预先承诺每周练几次，也不要求用户服从固定周期计划。每次用户真正准备训练时，产品根据最近实际训练、今天的时间和状态，重新安排一份可以立刻执行的单日计划。

产品不是泛化的“AI 私教”，也不是聊天机器人。它解决的是一个具体时刻：

> 我已经到了健身房，但不想研究中断后的计划应该怎么续。告诉你今天的现实情况，你直接安排我接下来怎么练。

完整价值闭环：

```text
首次建立极简基础资料
→ 用户到达健身房
→ 语音说今天想怎么练，或复用上次输入理解
→ 产品结合历史生成单日计划
→ 用户确认或快捷调整
→ 结构化页面陪伴执行和记录
→ 训练中按现场变化重排剩余计划
→ 生成事实型总结
→ 下一次真正使用本次数据
```

产品真正需要建立的长期能力是：

```text
低摩擦训练记录
+ 对不规律训练的编排规则
+ 用户真实调整反馈
+ 越用越可信的动作能力模型
+ 不制造负罪感的产品关系
```

AI 是实现自然语言理解和调整的手段，不是产品价值本身。

---

## 2. 用户与场景

### 2.1 目标用户

- 有过健身房经历，知道常用器械如何操作；
- 理解组、次数、重量和基本训练概念；
- 不想请长期私教，也不愿长期研究训练编排；
- 一周可能训练 0–5 次，容易被加班、出差和生活打断；
- 经常到健身房以后才决定今天练什么；
- 目标主要是维持肌肉、一般健康、基础体能或缓慢进步；
- 希望得到温和、具体、非评判式的陪伴。

首个测试用户是创始人本人：Android / OPPO 用户，有基础器械经验，训练会中断，偏好力量加有氧，有腰部劳损历史，不喜欢鸡血式督促。

### 2.2 非目标用户

MVP 不重点服务：

- 完全不会使用器械的新手；
- 竞技健美、力量举、奥举运动员；
- 需要医疗监督、伤后康复或诊断的人；
- 主要需求是饮食减脂的人；
- 未成年人；
- 需要实时动作安全保证的人。

### 2.3 核心任务

1. 到达健身房但不知道练什么：根据真实历史和今天的状态直接安排。
2. 计划被生活打断：不补课、不追赶，从今天重新接上。
3. 现场情况变化：器械被占、时间缩短、重量偏重或身体不适时，快速调整剩余计划。
4. 想知道是否进步：用重量、次数、体感和肌群覆盖给出事实，而不是连续打卡。

---

## 3. 产品原则

1. 计划适应用户，而不是用户服从计划。
2. 没有断签，只有下一次训练。
3. 输入成本必须显著低于用户自己编排计划的成本。
4. 语音负责低成本表达，对话负责理解，专用页面负责执行。
5. 每次真实训练比未来承诺更重要。
6. 数据可以不完整，核心体验不能残缺。
7. 鼓励必须基于事实，不做空泛吹捧。
8. Memory 可见、可改、可删。
9. 训练过程本地优先，断网可用。
10. 安全边界由确定性规则承担，不由 LLM 自由发挥。
11. 保持克制，不扩展成饮食、社区、视频和健康数据综合平台。

---

## 4. 输入成本设计

输入成本是产品成败的首要体验指标。产品不得要求用户为了获得计划，先完成复杂问卷或反复填写结构化表单。

### 4.1 首次使用

首次只要求最少基础信息：

- 性别；
- 身高；
- 体重；
- 大致训练经验；
- 默认训练环境；
- 已知的身体限制或明确不做的动作，可跳过。

主要目标、动作偏好、训练方式和沟通偏好，优先通过一次简短语音自然表达，例如：

> 我主要想维持肌肉和体能，平时用器械比较多，练完喜欢做一点有氧，腰以前有劳损，不想做高负重俯身动作。说话直接一点，不要鸡血。

语音转写后展示简短的“我理解的是”：

```text
目标：维持肌肉和基础体能
偏好：器械优先，力量后短有氧
限制：腰部有历史不适，避免高负重俯身
沟通：平静、具体
```

用户可以直接确认，也可以修改其中一项。不得要求用户逐项重新填写语音里已经表达过的信息。

目标：首次打开到第一份可执行计划不超过 90 秒。

### 4.2 老用户每天开练

首页的主操作只有：

- **语音说今天怎么练**；
- **沿用上次的训练意图**；
- 存在未完成训练时显示**继续训练**。

典型语音输入：

> 今天四十分钟，状态一般，不想练腿。

> 一周没练了，今天七十分钟，想练全身再做一点有氧。

> 和上次差不多，但今天只有半小时。

系统从语音中尽可能提取：

- 可用时间；
- 精力和主观状态；
- 当前疼痛或不适；
- 想练或不想练的部位；
- 当前器械环境；
- 其他临时要求。

只有无法安全推断且确实影响计划时才追问。缺失的普通信息使用默认值或历史值，不强迫用户补齐整张表。

### 4.3 复用上一次输入理解

产品保存“上一次训练意图的结构化理解”，而不是简单复制原始语音。

用户选择沿用时，先展示一句可修改摘要：

> 沿用上次：商业健身房、约 60 分钟、全身力量后短有氧。今天有变化吗？

用户可以：

- 直接生成；
- 语音补一句变化；
- 快捷修改时间；
- 标记今天有不适。

当前疼痛、精力和临时时间不能永久写成长期偏好；历史限制和稳定偏好可以参与下一次计划。

### 4.4 训练中的输入

完成一组默认只需一次点击。重量和次数默认继承上一组或上次训练，用户需要时再修改。

体感不是每组强制填写：

- 默认不打断；
- 重量明显需要调整时选择“轻松 / 合适 / 偏重”；
- 动作完成后可补一次整体体感。

训练中支持一句话调整：

- “腿举被占了”；
- “这组太重”；
- “临时只剩十五分钟”；
- “肩膀不舒服，剩下别练肩”。

目标：老用户从首页到计划出现不超过 20 秒；完成一组的常规操作不超过 5 秒。

语音不可用、权限拒绝或识别失败时，始终提供同等可用的文字输入和快捷选项。原始录音不保存。

---

## 5. 核心用户流程

### 5.1 首页

首页展示：

- 语音开练主按钮；
- 沿用上次输入；
- 未完成训练的继续入口；
- 上次训练日期和一句摘要；
- 一条有数据支持的近期变化。

首页禁止展示：

- 连续打卡天数；
- 本周还差几次；
- 未完成红点；
- 失败、落后、补课或惩罚性提示。

### 5.2 计划确认

计划确认页展示：

- 今日目标；
- 预计时长；
- 强度说明；
- 最多 2–3 句生成理由；
- 动作顺序；
- 组数、次数、休息；
- 上次记录或探索重量提示；
- 每个动作一句关键提示。

当前 Demo 与自动化验收采用同一份确定性样例：用户输入 70 分钟时，返回 68 分钟计划，包含 5 个动作、11 个力量组（3+3+3+2）和 15 分钟跑步机快走。页面汇总必须由动作明细计算，不允许单独写死另一套数字。

快捷调整：

- 轻松一点；
- 缩短到某个时长；
- 替换动作；
- 删除动作；
- 添加或取消有氧；
- 直接用语音表达变化。

动作替换必须进入统一动作库，而不是直接随机替换。动作库至少支持按目标肌群、器械类型和关键词筛选；动作详情展示起止阶段演示、中文步骤、关键提示、难度和建议参数。首批胸部动作覆盖器械、哑铃、杠铃和自重四类，并保留数据源与媒体许可证记录。

### 5.3 训练执行

当前动作页面展示：

- 动作名称和当前组数；
- 目标次数；
- 上次重量、次数和体感；
- 默认重量和次数；
- 完成本组；
- 自动休息计时；
- 简短动作提示。

支持：

- 替换动作；
- 跳过动作；
- 减少一组；
- 缩短剩余时间；
- 提前结束；
- 添加备注。

每次操作立即写入本地数据库。杀掉 App 后必须恢复到正确训练、动作、组数和休息时间。

### 5.4 总结

总结只描述可以由数据支持的内容：

- 实际时长；
- 完成动作和组数；
- 肌群覆盖；
- 有氧时长；
- 与历史同动作的一个真实对比；
- 一条事实型鼓励；
- 值得记住的偏好或限制候选。

示例：

> 今天完成了 15 个有效组。高位下拉在相同 40 kg 下比上次多完成 3 次。考虑到一周没有训练，本次保持余力是合理的。

提前结束时保存全部已完成内容，不得将其描述为失败。

### 5.5 历史与 Memory

历史提供：

- 单次训练列表和详情；
- 动作重量、次数和体感；
- 训练时长和肌群覆盖；
- 简单趋势。

Memory 只保存会影响未来决策的稳定信息：

- `preference`：偏好器械、力量后有氧；
- `constraint`：腰部历史不适、避免某动作；
- `habit`：周末通常时间充足；
- `context`：常用商业健身房；
- `communication`：喜欢温和或直接的语气。

每条 Memory 包含来源、置信度和状态，用户可以查看、编辑和删除。一次临时表达不能直接变成永久偏好。

动作能力独立结构化保存：最近重量、次数、体感、训练时间和置信度，不混入自然语言 Memory。

---

## 6. 训练编排规则

MVP 提供普通健身房力量训练建议，不提供医疗诊断、康复处方或竞技训练计划。

### 6.1 时间预算

- 20–30 分钟：3–4 个高价值动作；
- 40–50 分钟：4–5 个力量动作；
- 60–75 分钟：5–7 个力量动作，可选短有氧；
- 80–100 分钟：全身力量加有氧。

预计总时长不得明显超过用户可用时间。

### 6.2 中断恢复

- 较长时间未训练时降低训练量和强度；
- 不做补偿性加量；
- 优先全身覆盖；
- 每组保留一定余力；
- 不测试极限。

### 6.3 重量推荐

- 新动作不给精确重量，只提示逐级探索；
- 有历史时展示上次重量、次数和体感；
- 连续两次轻松完成后才建议小幅增加；
- “偏重”后下调后续建议；
- 缺失数据不能当作零。

### 6.4 动作选择

动作只能来自版本化本地目录。选择时考虑：

- 最近训练时间和近期有效组数；
- 用户当下想练或不想练的部位；
- 当前不适和长期限制；
- 可用时间和器械环境；
- 动作熟悉度和历史能力；
- 用户经常替换或跳过的动作。

动作目录至少覆盖胸、背、股四头、腘绳肌、臀、肩、二头、三头、核心和有氧，并包含稳定 slug、肌群、器械、提示、替代动作和风险标签。

### 6.5 安全停止

出现以下输入时不生成正常计划，提示停止训练并寻求适当帮助：

- 胸痛；
- 明显眩晕或晕厥；
- 异常呼吸困难；
- 急性关节或脊柱锐痛；
- 明显放射痛或麻木；
- 用户明确描述严重伤病。

产品只提供一般健身建议，不做诊断。

---

## 7. 功能范围

### 7.1 第一阶段：超窄本地闭环

第一阶段不接真实 LLM、不创建 Go 服务、不做账号和云同步。

必须实现：

- 极简基础资料；
- 文字训练意图输入；
- 可复用的上次输入理解；
- 确定性本地 Planner；
- 计划确认和快捷调整；
- 训练执行、计时和即时保存；
- 异常退出恢复；
- 本地事实型总结；
- 基础历史和动作能力；
- 可编辑 Memory；
- 版本化 JSON 导出。

语音接口在本阶段预留，是否立即实现以真机环境为准；文字流程不得因此被阻塞。

### 7.2 第二阶段：语音优先体验

- 系统语音转文字；
- 转写预览和编辑；
- 从自然语言提取结构化训练意图；
- 用一句语音修改上次输入；
- 健身房单手操作、大按钮和高对比优化；
- 权限拒绝、识别失败和不可用时降级文字；
- 不保存原始音频。

### 7.3 第三阶段：真实 AI 网关

只有真实训练验证通过后才接入：

- Go AI 网关；
- DeepSeek 或其他 OpenAI-compatible Provider；
- 自然语言计划生成和调整；
- AI 事实型总结；
- 安装令牌、邀请码和基础限流；
- 请求失败时回退本地 Planner。

### 7.4 明确不做

- Apple Health、Health Connect 和厂商健康数据；
- 视频动作分析和摄像头监督；
- 饮食和卡路里；
- 社区、排行榜和连续打卡；
- 手表端；
- 多设备和云同步；
- 手机号账号；
- 支付系统；
- 复杂推送；
- 医疗诊断和康复处方；
- 多 Agent 框架。

---

## 8. 技术方案

### 8.1 客户端

```text
Flutter / Dart
Riverpod
go_router
Drift + SQLite
Dio
flutter_secure_storage
系统 Speech-to-Text
Material 3，深色优先，中文 UI
```

Android-first，iOS 保持可编译。采用 feature-first 目录，但不为形式创建空层。

本地 SQLite 是训练业务主数据源，不是缓存。至少保存：

- 基础资料和 Memory；
- 上一次训练意图及结构化理解；
- 动作目录；
- Check-in；
- 训练计划和调整记录；
- 每一组重量、次数和体感；
- 动作能力和肌群状态；
- 当前训练恢复位置；
- 历史总结。

每次训练操作的顺序：

```text
先事务写入 SQLite
→ 更新本地状态和 UI
→ 再触发非关键异步逻辑
```

活动训练分两层状态：

- Session：`draft / ready / active / paused / completed / ended_early`；
- 执行阶段：`ready / active / resting / paused`。

休息计时持久化绝对结束时间，不只保存倒计时数字。

### 8.2 本地 Planner

定义稳定接口，使本地和远程实现可替换：

```dart
abstract interface class WorkoutPlanner {
  Future<WorkoutPlan> generate(WorkoutPlanningContext context);
  Future<WorkoutPlan> adjust(
    WorkoutPlan current,
    WorkoutAdjustment adjustment,
  );
}
```

本地 Planner 必须确定性、可测试、只使用本地动作目录，并覆盖时间预算、停训减量、不适过滤、偏好排序、重量推荐和现场调整。

### 8.3 Go AI 网关

第三阶段采用：

```text
Go 单体服务
标准库 net/http
database/sql + modernc.org/sqlite
log/slog
环境变量配置
Docker Compose + Caddy
首尔单机部署
```

API 范围：

```text
GET  /healthz
POST /v1/installations
POST /v1/coach/plans
POST /v1/coach/adjustments
POST /v1/coach/summaries
```

封闭测试使用邀请码和安装令牌，不做账号。令牌至少使用 32 字节随机值，客户端安全存储，服务端只保存 SHA-256 hash。

服务端 SQLite 只保存邀请码、安装实例、配额、Prompt 版本和 AI 调用元数据。训练历史仍以手机本地为主。

### 8.4 AI 边界

规则层负责：

- 时间预算；
- 候选动作和动作白名单；
- 不适与限制过滤；
- 停训减量；
- 组数和总训练量；
- 新动作不推荐精确重量；
- 高风险输入停止正常计划；
- 总时长估算；
- 禁止焦虑文案。

LLM 负责：

- 理解用户自然语言；
- 在候选动作内组合；
- 将调整意图转换为结构化变更；
- 给出简短解释；
- 根据客户端提供的事实生成总结。

模型返回必须经过：

```text
严格 JSON 解码
→ 字段和范围校验
→ 动作目录校验
→ 用户限制校验
→ 时长和训练量校验
→ 确定性修复或拒绝
```

模型名通过环境变量配置，不写死在业务代码。关闭 API、请求超时、网络失败或模型输出非法时直接回退本地 Planner，不使用未经完整规则复验的“上一次计划安全变体”。

### 8.5 数据和隐私边界

国内 MVP 不把隐私作为核心卖点或主要产品阻力，但遵守必要底线：

- 不保存或上传原始语音；
- 模型 Key 不进入客户端；
- 生产日志不记录 access token、完整原始语音、模型正文和不必要的完整训练历史；
- 发给 AI 的内容限于本次计划和总结所需信息；
- 用户可以查看和删除 Memory，并清空本地数据；
- 提供版本化 JSON 导出，导入能力在真实需求出现后补齐。

为了验证产品，可以上报匿名、有限枚举的行为事件，例如计划生成、开始训练、完成训练、替换动作、重量偏重、恢复成功和本地回退。事件统计不阻塞训练流程。

---

## 9. 实施顺序与阶段门槛

### Phase 0：工程骨架

- 创建 `apps/mobile` Flutter 项目；
- 配置主题、路由、Riverpod、Drift 和测试；
- 建立版本化动作目录；
- Android 模拟器和 OPPO 真机可启动；
- `flutter analyze`、`flutter test` 和 debug APK build 通过。

### Phase 1：本地真实训练闭环

依次实现：

```text
极简基础资料
→ 训练意图输入和复用
→ 本地计划
→ 计划确认
→ 训练执行
→ 异常恢复
→ 总结和基础历史
```

先完成 happy path，再补替换、缩短、提前结束、异常恢复和导出。

### Phase 2：真实用户体验验证

进入远程 AI 前必须满足：

- 创始人在不同场景完成至少 3 次真实训练；
- 至少 3 名非创始人目标用户完成第二次训练；
- 大部分计划无需大改即可开始；
- 没有用户因为输入流程太长放弃开始；
- 常规完成一组的中位操作时间小于 5 秒；
- 至少出现一次用户主动感知“它记住了我”；
- 杀进程恢复和全程断网训练可靠。

未满足时先修正定位、交互和规则，不建设 Go AI 网关。

### Phase 3：语音与 AI

- 先把语音开练和复用上次输入做顺；
- 再创建 Go API、安装令牌和 Provider；
- Flutter 使用 Feature Flag 选择远程 Planner；
- 远程成功后把计划保存本地，执行阶段不依赖网络；
- 远程失败自动回退本地 Planner；
- 10–20 名邀请码用户进入封闭测试。

### Phase 4：由真实需求触发

只有留存和付费意愿得到验证后才考虑账号、云同步、换机恢复、PostgreSQL、正式支付、健康数据和视频动作分析。

---

## 10. 验收场景

### 10.1 首次使用与输入

- 首次只填写最少基础资料，并可用一段语音表达目标、偏好和限制；
- 转写内容可编辑，已经识别的信息不要求再次逐项填写；
- 重启后不重复首次流程；
- 老用户可一键复用上次训练意图并补充今天的变化；
- 语音权限拒绝时文字流程完整可用，且不反复弹权限。

### 10.2 本地计划

- 一周以上未练、90 分钟：生成恢复型全身计划和可选有氧，不测试极限；
- 只有 25 分钟：包含 3–4 个高价值动作，预计时长不明显超出；
- 当前腰部不适：过滤高负重俯身和明显增加腰部负担的动作，不做诊断；
- 不想练腿：腿部优先级下降，允许继续跳过；
- 新动作无精确公斤数，只提示逐级探索；
- 所有动作 slug 均存在于动作目录。

### 10.3 训练执行

- 完成一组后数据立即出现在 SQLite，并进入休息阶段；
- 第三个动作第二组休息时杀进程，重启后回到同一训练、动作和组数；
- 替换被占用的器械时只修改未完成部分，已完成记录不被重置；
- 临时只剩 15 分钟时保留高优先级动作，删除或减少低优先级动作；
- 提前结束保存全部已完成内容，总结不描述为失败；
- 全程断网时训练执行、计时、总结和历史仍可用。

### 10.4 总结和历史

- 同重量比上次多完成 3 次时可以描述该事实；
- 没有可比历史时只描述本次完成情况，不虚构进步；
- 完成训练后更新动作最近重量、次数、体感和时间；
- 用户删除某条 Memory 后，它不再参与后续计划；
- 导出 JSON 包含基础资料、Memory、历史、动作能力和 Schema 版本。

### 10.5 远程 AI

- 模型 Key 不出现在 APK；
- 无效 Token 返回统一 401，不泄漏细节；
- 非法 JSON、未知字段、动作库外动作和超量计划不能进入 App；
- 高风险症状返回停止建议而不是普通计划；
- 请求超时后保留用户输入并回退本地 Planner；
- 生产日志不包含 token、原始音频、完整模型正文和不必要的完整训练历史；
- 关闭 API 后仍能完成整场训练。

### 10.6 文案

面向用户的文案不得出现：

- 断签；
- 失败；
- 必须坚持；
- 补课；
- 战胜懒惰；
- 本周还差 X 次。

---

## 11. 指标与继续投入标准

早期北极星指标：

> 由产品辅助完成的有效训练次数。

有效训练定义由短时和常规训练共同决定，至少要求用户真实开始多个动作或完成足够的有效组，并成功保存训练结果。

关键指标：

1. 计划生成后 3 分钟内开始训练率；
2. 开始后完成总结率；
3. 第二次和第四次训练完成率；
4. 30 天内完成至少 4 次训练的用户比例；
5. 首次计划无需替换即可开始的比例；
6. 平均动作替换数；
7. 推荐重量偏重或偏轻比例；
8. 常规完成一组的操作耗时；
9. 异常退出恢复成功率；
10. 用户是否认为第二次比第一次更懂自己；
11. 20 名种子用户中真实付费或支付订金的人数。

早期继续投入标准：

- 20 名目标用户中至少 10 人完成 4 次训练；
- 至少 5 人以真实支付、订金或明确购买行为表达付费意愿；
- 若未达到，先调整定位、输入流程和计划质量，不扩展视频、健康数据和综合健身功能。

---

## 12. 工程完成定义

每个实施阶段结束时必须：

- 运行格式化、静态检查和相关测试；
- 构建 Android debug APK；
- 在 OPPO 真机执行本阶段关键场景；
- 汇报已完成内容、运行方法、真实测试结果和已知限制；
- 不声称未实际运行或未测试的内容已经通过；
- 不提交模型密钥、生成 APK、本地数据库和个人配置；
- 不在当前阶段顺手建设账号、云同步、支付或其他范围外功能。
